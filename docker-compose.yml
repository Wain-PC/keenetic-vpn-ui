services:
  keenetic-vpn-ui:
    build: .
    ports:
      - "3000:3000"
    environment:
      ROUTER_IP: ${ROUTER_IP:-192.168.1.1}
      ROUTER_USER: ${ROUTER_USER:-admin}
      ROUTER_PASS: ${ROUTER_PASS}
      VPN_POLICY: ${VPN_POLICY:-Policy0}
    restart: unless-stopped
